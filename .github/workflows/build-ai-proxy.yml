name: Build AI Proxy Rules

on:
  workflow_dispatch:

  schedule:
    - cron: '30 3 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build auto-ai-proxy.list
        shell: bash
        run: |
          set -e

          mkdir -p loon

          cat > loon/auto-ai-proxy.list <<EOF
          # NAME: MyAI
          # AUTHOR: kgh
          # REPO: https://github.com/hk-js/shuntingRule
          # GENERATED BY GITHUB ACTION
          # UPDATED: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          EOF

          while IFS= read -r url; do
            [ -z "$url" ] && continue
            [[ "$url" =~ ^# ]] && continue

            echo "" >> loon/auto-ai-proxy.list
            echo "# SOURCE: $url" >> loon/auto-ai-proxy.list

            if curl -fsSL "$url" \
              | grep -v '^#' \
              | grep -v '^$' \
              >> loon/auto-ai-proxy.list
            then
              echo "Loaded: $url"
            else
              echo "# FAILED: $url" >> loon/auto-ai-proxy.list
            fi
          done < rules/ai-proxy-source.txt

          awk '!seen[$0]++' loon/auto-ai-proxy.list > loon/auto-ai-proxy.tmp
          mv loon/auto-ai-proxy.tmp loon/auto-ai-proxy.list

      - name: Commit Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add loon/auto-ai-proxy.list

          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "auto update ai rules"
            git push
          fi
